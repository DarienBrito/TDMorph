# ElementsContainerPanel

## Core level methods

### Promoted

```python
CalculateHeight()
```
Sets the container's height based on elements.

```python
OnAction(name)
```
Runs whenever there's a panel action. "name" is panel action name.

```python
SetDefaultPinnedSize()
```
Sets the default size of the container when "pinned".
